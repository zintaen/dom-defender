// Auth.js v5: the configuration now lives in the root `auth.ts`. This file is
// kept only as a compatibility re-export. Import { auth } from "@/auth" directly.
export { auth, handlers, signIn, signOut, authConfig } from "@/auth";
