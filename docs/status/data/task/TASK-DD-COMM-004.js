window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-DD-COMM-004"]="<h2>Section 1 - behavior</h2>\n<p>On a replay page, signed-in players can add an emoji reaction (toggle) and a short comment. Comments and the display text run through the NFR-DOM-007 moderation gate (denylist + length + rate limit) and are sanitized before render. Authors can delete their own comments; abusive content can be reported. Reaction and comment counts show on the replay and feed cards.</p>\n<h2>Section 4 - acceptance criteria</h2>\n<ol><li>A signed-in user can react (toggle) and post a comment; an unauthenticated user cannot.</li><li>Comment text is sanitized (no XSS) and passes the moderation gate; a denylisted or over-rate-limit comment is rejected.</li><li>A user can delete their own comment; reporting flags content for review.</li><li><code>npx tsc --noEmit</code>, <code>npm run lint</code>, <code>npm test</code> clean.</li></ol>\n<h2>Section 5 - test plan</h2>\n<ul><li>Unit: sanitizer strips markup; moderation gate rejects denylisted text and enforces the rate limit; reaction toggle is idempotent per user.</li><li>Integration: post -&gt; render -&gt; delete; report path flags the item.</li></ul>\n<h2>Section 7 - dependencies and notes</h2>\n<p>Depends on TASK-DD-COMM-001 (identity) and NFR-DOM-007 (moderation). Reuse the rate-limit primitive built in NFR-DOM-003 here rather than writing a third one.</p>";
