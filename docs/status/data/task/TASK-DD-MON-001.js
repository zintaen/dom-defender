window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-DD-MON-001"]="<h2>Section 1 - behavior</h2>\n<p>Wire Stripe Checkout behind the existing <code>PRO_BILLING_ENABLED</code> flag, keeping the fail-closed contract: when the flag is off or <code>STRIPE_SECRET_KEY</code> is missing, the usd path keeps returning 503 and the UI points to the waitlist (today's behavior). When configured, <code>POST /api/shop/ purchase</code> with <code>method: \"usd\"</code> creates a Stripe Checkout Session and returns its URL.</p>\n<p>A webhook at <code>/api/billing/webhook</code> verifies the Stripe signature and, on <code>checkout.session.completed</code> / subscription events, sets the user's Pro state. Pro is granted only by a verified webhook, never by the client. <code>requirePro()</code> in <code>lib/pro.ts</code> then resolves against the stored subscription state and gates Pro cosmetics and modes (TASK-DD-MON-002).</p>\n<h2>Section 4 - acceptance criteria</h2>\n<ol><li>Flag off or key missing: usd purchase returns 503 with the waitlist pointer (unchanged).</li><li>Flag on + key set: usd purchase returns a valid Checkout Session URL.</li><li>A webhook with a bad or missing signature is rejected with 400 and grants nothing. Verified by <code>billing.test.ts</code>.</li><li>A valid <code>checkout.session.completed</code> marks the user Pro; a cancellation clears it.</li><li>Pro can never be set from a client request - only from a verified webhook (test asserts).</li><li><code>npx tsc --noEmit</code>, <code>npm run lint</code>, <code>npm test</code> clean. No secret committed.</li></ol>\n<h2>Section 5 - test plan</h2>\n<ul><li>Unit: signature verification rejects forged payloads; the grant/revoke reducer maps events to Pro state correctly.</li><li>Integration: full path with the flag off (503) and on (session URL); webhook grant then the user reads as Pro through <code>requirePro</code>.</li></ul>\n<h2>Section 7 - dependencies and notes</h2>\n<p>Phase P2, after the P0 hardening and a P1 audience exist. Depends on NFR-DOM-001 (do not sell status on a fakeable leaderboard) and NFR-DOM-004 (security headers / HSTS before taking card flows, even though Stripe hosts the card form). Test mode end to end with Stripe test keys before the flag is flipped in production; flipping the flag is an operator action (a stop condition under AGENTS.md).</p>";
