window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-DD-EDU-001"]="<h2>Section 1 - behavior</h2>\n<p>A host creates a room: a shared server-derived seed, a time box, and a join link. Participants join the link, play the identical seed, and a live team board ranks them. When the time box ends, an after-action summary shows the team's results plus, for each player, the AI coach tips (TASK-DD-AI-002), and each bug type is annotated with a one-line real-web-concept explainer (drift = layout/CSS, console popups = error handling, leaks = memory management). The result is a playable team event that doubles as a teaching session: dev onboarding, a hiring-screen warmup, a meetup or hackathon activity. Room scores are validated server-side like every other mode.</p>\n<p>Positioning: this is the CyberSkill lead-magnet surface. Keep a light \"run this for your team\" path that points to CyberSkill, and leave room for a Pro/B2B gate later (TASK-DD-MON-002) without blocking the free single-room flow.</p>\n<h2>Section 4 - acceptance criteria</h2>\n<ol><li>A host creates a room and gets a join link; multiple participants join the same seed.</li><li>The team board ranks participants live; the seed is server-derived and identical for all.</li><li>At time-box end, the summary shows team results, per-player coach tips, and the concept map.</li><li>Team scores pass NFR-DOM-001 validation; forged scores are rejected.</li><li><code>npx tsc --noEmit</code>, <code>npm run lint</code>, <code>npm test</code> clean.</li></ol>\n<h2>Section 5 - test plan</h2>\n<ul><li>Unit: room create/join/close transitions; seed is stable per room and server-derived; board rollup ranks correctly.</li><li>Integration: two members join, both play the same seed, board ranks them, summary renders the concept map and coach tips.</li></ul>\n<h2>Section 7 - dependencies and notes</h2>\n<p>Depends on NFR-DOM-001 (a team event needs a trustworthy board) and TASK-DD-COMM-001 (identity). Reuses the seed machinery (daily/tournament), the leaderboard pattern, and the AI coach. This is the strategic bet in the roadmap: build it after the core P1 loops exist, then use it in CyberSkill outreach. A Vietnamese room (TASK-DD-REACH-002) is a strong local-market wedge.</p>\n<h2>Section 8 - implementation status (2026-06-24, scaffolded)</h2>\n<p>Built and unit-tested:</p>\n<ul><li>lib/game/room.ts: makeRoomCode, isValidRoomCode, roomSeed (deterministic via seedFromDateKey), rankMembers (standard competition ranking 1,2,2,4). 6 tests green.</li><li>lib/game/conceptMap.ts: CONCEPT_MAP (drift/error/leak/boss), explainBug, allConcepts - the after-action teaching layer. 3 tests green.</li><li>models/Room.ts: code (unique), host, seed, status, timeBoxMinutes, members[], 24h TTL.</li><li>app/api/teams/route.ts: POST create/join/submit/close + GET standings, all behind auth().</li><li>app/teams/page.tsx: create-a-room (time box) + join-by-code.</li><li>app/teams/[room]/page.tsx: live standings poll, copy-join-link, \"Play this room\" (/play?seed={roomSeed}), manual score submit, host close, closed-state concept-map recap.</li></ul>\n<p>Known gaps to harden before this goes in front of a paying workshop:</p>\n<ul><li>The submit action stores a client-reported score. Route it through the NFR-DOM-001 replay-validated path (the same way /api/scores validates a run) so a room board cannot be gamed. This is the one real correctness debt in the slice and is marked with a NOTE in the route.</li><li>\"Play this room\" hands the seed via query string; later, stamp the room code on the submitted run server-side so a score can only count for the room it was actually played in.</li><li>Verification is sandbox-limited to the pure cores. tsc/lint/build on the next-16 branch is the real gate for the React pages and the Mongoose model.</li></ul>";
