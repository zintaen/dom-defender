window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-DD-SOC-002"]="<h2>Section 1 - behavior</h2>\n<p>Add a server-rendered Open Graph image at <code>/api/og</code> (using next/og) that draws a run card: score, wave reached, skin/theme, username, and a \"Beat my seed\" call to action on the run's accent gradient. Wire OG and Twitter card meta tags on the replay, challenge, and profile pages so links unfurl with that image in chats, Slack, Discord, and on social. Keep the existing copyable text summary. The image takes only display values from the query and never embeds PII.</p>\n<h2>Section 4 - acceptance criteria</h2>\n<ol><li><code>/api/og?...</code> returns a valid 1200x630 image showing score, wave, skin, and the CTA.</li><li>A challenge URL, a replay URL, and a profile URL each unfurl with the image (validated with a card validator / a fetch of the meta tags).</li><li>The image and its query contain no email or internal id (test asserts).</li><li><code>npx tsc --noEmit</code>, <code>npm run lint</code>, <code>npm test</code> clean.</li></ol>\n<h2>Section 5 - test plan</h2>\n<ul><li>Unit/route: the OG endpoint returns an image response with the expected fields rendered.</li><li>Meta: each page emits correct <code>og:image</code> / <code>twitter:image</code> tags pointing at the endpoint.</li></ul>\n<h2>Section 7 - dependencies and notes</h2>\n<p>Depends on TASK-DD-SOC-001 (the challenge links this decorates). This is the highest return-per-hour growth item in P1 because it makes every shared link visible.</p>\n<h2>Section 8 - implementation status (2026-06-24, scaffolded)</h2>\n<p>Built and unit-tested (8 tests green):</p>\n<ul><li>lib/og/ogParams.ts: parseOgParams (the trust boundary - accepts only name/score/wave/skin/ mode/cta, clamps the numbers, allowlists skin + mode, strips control characters, and replaces any email-like name with \"A player\" so no PII reaches the image or its URL) and buildOgQuery (constructs the /api/og query from typed values, re-sanitized). tests/og.test.ts covers clamping, fallbacks, the PII guard, and a buildOgQuery -&gt; parseOgParams round-trip.</li></ul>\n<p>Endpoint + UI (build-verified on the next-16 branch, not in the sandbox):</p>\n<ul><li>lib/og/runCard.tsx: the 1200x630 satori layout (score, wave, name, per-skin accent, CTA). Inline styles only, imports nothing from next/og.</li><li>app/api/og/route.tsx: GET returns an ImageResponse of runCard(parseOgParams(query)).</li><li>app/challenge/[token]/page.tsx and app/replay/[id]/page.tsx: generateMetadata now emits og:image + twitter summary_large_image pointing at /api/og with an absolute URL built from the request host.</li></ul>\n<p>Deferred / follow-ups:</p>\n<ul><li>Profile OG (app/u/[username]) is listed in this spec but the profile page does not exist yet; it lands with TASK-DD-COMM-001 (public profiles), which should reuse buildOgQuery + /api/og.</li><li>The tournament page is a client component, so it cannot export generateMetadata; a small server wrapper (or a route segment) can add its OG card later.</li><li>Runtime risk to confirm on deploy: next/og ImageResponse renders with the built-in font on the Node runtime. If the platform needs the Edge runtime or an explicit font, set it on the route. This is a render-time concern, not a build/tsc one.</li><li>components/ShareCard.tsx in the original envelope does not exist; the in-game share card lives in components/PlayShell.tsx (SubmitToast) and already carries the copyable text, so no change was needed there.</li></ul>";
