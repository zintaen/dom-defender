window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-DD-AI-001"]="<h2>Section 1 - behavior</h2>\n<p>Add a difficulty director that adjusts the bug spawn rate and the bug-type mix to the player's measured skill, in real time, in endless mode only. Daily, tournament, and any seeded run keep their deterministic spawn sequence untouched, so leaderboard fairness is preserved (this is the protected-area constraint from AUDIT-CONFIG).</p>\n<p>The director is a pure function: it takes a <code>PlayerSkillSnapshot</code> (recent fix latency, current combo, crash-meter trend, misses) and returns a <code>DirectorDecision</code> (spawn interval multiplier, bug-type weights, whether to allow a boss this wave). v1 uses a transparent heuristic. The function signature is the AI seam: TASK-DD-AI-002 and a later server model can return the same <code>DirectorDecision</code> shape from a learned policy without touching the caller.</p>\n<p>The director only widens within tuned bounds (it cannot make the game unwinnable or trivial), and it is disabled when <code>mode !== \"endless\"</code>.</p>\n<h2>Section 4 - acceptance criteria</h2>\n<ol><li>In endless mode, sustained high performance (fast fixes, high combo) raises the spawn rate within bounds; sustained struggling lowers it. Verified by <code>director.test.ts</code>.</li><li>In daily, tournament, or any seeded run, the director is bypassed and the spawn sequence is byte-for-byte identical to today's behavior. Verified by a determinism test that runs the same seed twice and diffs the spawn log.</li><li>The director never returns a spawn interval outside <code>[MIN_MS, MAX_MS]</code> or weights that sum to zero (property test).</li><li><code>npx tsc --noEmit</code>, <code>npm run lint</code>, and <code>npm test</code> are clean.</li></ol>\n<h2>Section 5 - test plan</h2>\n<ul><li>Unit: feed scripted skill snapshots, assert decisions move in the right direction.</li><li>Determinism: same seed in twice =&gt; identical spawn log (guards the protected area).</li><li>Property: random snapshots never produce out-of-bounds or degenerate decisions.</li></ul>\n<h2>Section 7 - dependencies and notes</h2>\n<p>Depends on nothing; it is safe to build before or after the hardening tasks because it does not touch auth, scoring, or the API. Keep the decision struct stable - TASK-DD-AI-002 (replay coach) and any future learned director reuse it. This task is the concrete \"AI\" hook in the CyberSkill story: a heuristic today, a model tomorrow, same seam.</p>";
