window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-DD-COMM-001"]="<h2>Section 1 - behavior</h2>\n<p>A public, read-only profile at <code>/u/&lt;username&gt;</code> showing the player's display name, best endless and daily scores, total runs, achievements, equipped cosmetics, and a short list of recent public replays (each links to the existing <code>/replay/[id]</code> viewer). The page is served from a public projection endpoint that returns only safe fields.</p>\n<p>Privacy is opt-out: a <code>profilePublic</code> flag (default true) on the user, toggled from the account page. When false, <code>/u/&lt;username&gt;</code> returns a \"this profile is private\" state and the projection omits the player.</p>\n<h2>Section 4 - acceptance criteria</h2>\n<ol><li><code>/u/&lt;existing-username&gt;</code> renders stats, achievements, cosmetics, and recent replays.</li><li>The public projection never includes email, passwordHash, or internal ids. Verified by <code>public-profile.test.ts</code> asserting the response shape.</li><li>Setting the profile private hides the page and removes the user from the projection.</li><li>An unknown username returns a clean not-found state, not a 500.</li><li><code>npx tsc --noEmit</code>, <code>npm run lint</code>, <code>npm test</code> clean.</li></ol>\n<h2>Section 5 - test plan</h2>\n<ul><li>Unit: projection function strips sensitive fields; private flag is honored.</li><li>Integration: public profile renders for a seeded user; private user is hidden; unknown user is a clean 404.</li></ul>\n<h2>Section 7 - dependencies and notes</h2>\n<p>No hard dependency, but it is the prerequisite for TASK-DD-COMM-002 (friends), -003 (guilds), -004 (replay reactions), and TASK-DD-SOC-004 (referrals). Build it early in P1. Reuses the data already on <code>User</code>, <code>Score</code>, and <code>Replay</code>; the main new surface is the safe projection.</p>\n<h2>Section 8 - implementation status (2026-06-24, scaffolded)</h2>\n<p>Built and unit-tested (9 tests green):</p>\n<ul><li>lib/profile/publicProfile.ts: projectPublicProfile (field-by-field projection - constructs only safe display fields so email / passwordHash / internal id can never leak even from a full document), isProfilePublic (opt-out: public unless profilePublic is explicitly false), normalizeUsername, isValidUsernameParam. tests/public-profile.test.ts asserts the PII guard, the safe fields, the opt-out default, and the username helpers.</li></ul>\n<p>Server + UI (build-verified on the next-16 branch, not in the sandbox):</p>\n<ul><li>models/User.ts: added profilePublic (default true) + optional displayName (max 32).</li><li>app/api/profile/[username]/route.ts: public projection GET. Case-insensitive username lookup; unknown -&gt; clean 404; private -&gt; { private: true } with no player data; public -&gt; projection plus best endless, best daily (from Score), and the 5 most recent replays (from Replay).</li><li>app/u/[username]/page.tsx: read-only profile (stats, best runs, achievement count, recent replays linking to /replay/[id]); private state; 404 via notFound. generateMetadata emits the og:image - this also closes the TASK-DD-SOC-002 profile-OG deferral, reusing buildOgQuery + /api/og.</li><li>app/api/profile/route.ts: PATCH extended to accept profilePublic + displayName; GET now returns them so the account page can show current state.</li><li>app/account/page.tsx: \"Public profile\" section with the visibility toggle (the opt-out, which must ship with default-public) and a display-name field.</li></ul>\n<p>Follow-ups:</p>\n<ul><li>Make profiles discoverable by linking usernames on the leaderboard and tournament boards to /u/[username] (small, build-safe; left out to keep this slice within its envelope).</li><li>COMM-002 (friends), -004 (reactions) now have their anchor and can build on this projection.</li><li>Verification is sandbox-limited to the pure projection core; tsc/lint/build on the next-16 branch is the real gate for the model, the two route changes, the page, and the account edit.</li></ul>";
