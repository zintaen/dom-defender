window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-DD-REACH-001"]="<h2>Section 1 - behavior</h2>\n<p>Make the full game playable with touch on a phone. Add an input abstraction so the three tools and four power-ups have pointer-driven equivalents: an on-screen tool switcher and power-up buttons (replacing hotkeys 1/2/3 and Q/W/E/R), tap-to-act for the debugger and garbage collector, and touch-drag for duct tape. The bug surface and HUD become responsive, the page locks scroll/zoom during a run, and no action depends on hover or a physical key.</p>\n<p>Game timing, spawn rates, and balance are unchanged - this is an input and layout change only, which keeps the daily and seeded runs comparable across desktop and mobile.</p>\n<h2>Section 4 - acceptance criteria</h2>\n<ol><li>A full run can be completed on a phone with thumbs only: switch tools, fire power-ups, fix every bug type, beat a boss.</li><li>No action requires hover or a keyboard; desktop keyboard play still works unchanged.</li><li>The play screen fits a phone viewport with no page scroll or pinch-zoom mid-run.</li><li>Input mapping is covered by <code>input.test.ts</code> (pointer event -&gt; intended game action).</li><li><code>npx tsc --noEmit</code>, <code>npm run lint</code>, <code>npm test</code> clean.</li></ol>\n<h2>Section 5 - test plan</h2>\n<ul><li>Unit: <code>lib/game/input.ts</code> maps pointer/touch events to the same actions the keys produce.</li><li>Manual: a real-device pass on one iOS and one Android phone (record in the PR).</li><li>Regression: desktop keyboard + mouse path unchanged.</li></ul>\n<h2>Section 7 - dependencies and notes</h2>\n<p>No hard dependency, but it multiplies the value of every share and challenge link, so build it in the same P1 slice as TASK-DD-SOC-001/002. Pairs with TASK-DD-REACH-003: large touch targets and visible focus help both touch and accessibility.</p>";
