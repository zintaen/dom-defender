window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-DD-SOC-003"]="<h2>Section 1 - behavior</h2>\n<p>A weekly tournament: a server-derived <code>weekKey</code> seeds one shared bug pattern for the whole week (reusing the daily-seed machinery). Players enter during the week, runs submit as mode \"tournament\" and are validated server-side (NFR-DOM-001), and a ranked board shows standings with a countdown to rollover. At week end the board freezes and the winners are surfaced; a new seed begins. The seed is always server-derived so no one can pre-practice a forged seed.</p>\n<h2>Section 4 - acceptance criteria</h2>\n<ol><li>Every player in a given week plays the identical tournament seed (determinism test).</li><li>Tournament scores go through the same server validation as the daily board; forged scores are rejected.</li><li>Week rollover is deterministic and the prior week's board freezes correctly.</li><li><code>npx tsc --noEmit</code>, <code>npm run lint</code>, <code>npm test</code> clean.</li></ol>\n<h2>Section 5 - test plan</h2>\n<ul><li>Unit: <code>weekKey()</code> and the derived seed are stable within a week and change at rollover.</li><li>Integration: a tournament submission validates; a forged one is rejected; standings rank correctly.</li></ul>\n<h2>Section 7 - dependencies and notes</h2>\n<p>Depends on NFR-DOM-001 (a competitive event on a fakeable board is worse than no event). Sets up FR-DD-OPS-001 (seasons + rotating modifiers), which generalizes this into recurring live ops.</p>\n<h2>Section 8 - implementation status (2026-06-24, scaffolded)</h2>\n<p>Built and unit-tested (8 tests green):</p>\n<ul><li>lib/game/tournament.ts: weekKey (ISO-8601 week, UTC, resolved via the week's Thursday so the year is correct at boundaries), tournamentSeed (reuses seedFromDateKey, unsigned 32-bit), weekStart / weekEnd / msUntilRollover. tests/tournament.test.ts proves the seed is stable within a week and changes at the Monday rollover.</li></ul>\n<p>Server + UI (build-verified on the next-16 branch, not in the sandbox):</p>\n<ul><li>models/Score.ts: mode enum and IScore union widened to include \"tournament\"; dailyKey holds the ISO week key for tournament rows (the existing { mode, dailyKey, score } index serves the weekly board).</li><li>app/api/scores/route.ts: accepts mode \"tournament\"; the week key and seed are derived server-side (never from the client); a supplied replay must carry the week seed or it is rejected; the run still goes through the NFR-DOM-001 replay validation, same as daily.</li><li>app/api/tournament/route.ts: GET returns weekKey, server seed, startsAt/endsAt, countdown, and the best-per-user ranked board for the week.</li><li>app/tournament/page.tsx: live board, countdown to rollover, \"Play this week\" entry.</li><li>components/PlayShell.tsx: accepts mode \"tournament\" and maps it to the seeded \"daily\" core path so the protected Game.tsx is untouched (seed set =&gt; director off =&gt; determinism holds); the real \"tournament\" mode is what is submitted and shared.</li><li>components/Nav.tsx: Tournament entry point.</li></ul>\n<p>Known gaps / follow-ups:</p>\n<ul><li>The Game HUD shows the \"DAILY\" badge during a tournament run because tournament maps onto the daily core path. A later clean pass widens Game.tsx's mode union to add a real TOURNAMENT badge. Cosmetic only; the score is recorded and ranked as a tournament.</li><li>Week rollover freeze is implicit: a closed week's rows stay queryable by their week key, but there is no surfaced \"winners\" archive view yet. Add a read-only past-weeks board when OPS-001 (seasons) lands.</li><li>Verification is sandbox-limited to the pure core. tsc/lint/build on the next-16 branch is the real gate for the route, page, model change, and PlayShell edit.</li></ul>";
