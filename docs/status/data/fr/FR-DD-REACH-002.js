window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-DD-REACH-002"]="<h2>Section 1 - behavior</h2>\n<p>Add a string catalog and a <code>t()</code> helper. English is the source-of-truth catalog; Vietnamese is a full translation. A locale switch (and the <code>lang</code> attribute) is set from the user's choice, falling back to the browser preference, then English. All visible UI, in-game, and share copy read from the catalog. Only natural-language copy is translated - seeds, identifiers, and API field names stay as is. Vietnamese typography and number format follow the locale.</p>\n<p>The migration is incremental: land the helper and the two catalogs, then convert screens key by key, so the work is shippable in small commits rather than one large rewrite.</p>\n<h2>Section 4 - acceptance criteria</h2>\n<ol><li>Switching to Vietnamese translates every visible string on the home, play, daily, leaderboard, account, and share surfaces; switching back restores English.</li><li>No missing-key fallbacks render a raw key to the user (test asserts EN and VI have the same key set).</li><li>Longer Vietnamese strings do not break the HUD or buttons (spot-checked on mobile widths).</li><li><code>npx tsc --noEmit</code>, <code>npm run lint</code>, <code>npm test</code> clean.</li></ol>\n<h2>Section 5 - test plan</h2>\n<ul><li>Unit: <code>en.json</code> and <code>vi.json</code> have identical key sets; <code>t()</code> returns the right locale value and never leaks a raw key.</li><li>Manual: VI pass on the core screens at a phone width.</li></ul>\n<h2>Section 7 - dependencies and notes</h2>\n<p>Pairs with FR-DD-REACH-001 (mobile) and FR-DD-EDU-001 (a Vietnamese teams mode is a strong local-market wedge for CyberSkill). next-intl is a reasonable library choice; a tiny in-repo dictionary is also fine given the small surface. Keep <code>en.json</code> the source of truth so new copy lands there first.</p>";
