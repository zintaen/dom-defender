window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-DD-AI-002"]="<h2>Section 1 - behavior</h2>\n<p>After a run, analyze its recorded replay and produce up to three concrete tips, each tied to an actual event in that run (for example: \"memory leaks in wave 4 caused 40% of your crash meter - the garbage collector was idle for 12s\"). v1 is a pure heuristic over replay stats, deterministic for a given replay, with no network call. The function reuses the AI seam from FR-DD-AI-001, so a later LLM-backed coach (behind a flag and the cost guard) can return the same <code>CoachTip</code> shape. Every tip must cite evidence from the run; no generic advice.</p>\n<h2>Section 4 - acceptance criteria</h2>\n<ol><li>The coach returns up to three tips, each with evidence drawn from the replay's events.</li><li>The same replay always yields the same tips (deterministic, testable).</li><li>A flawless run yields praise + at most one stretch tip, not invented faults.</li><li>No network call in v1. <code>npx tsc --noEmit</code>, <code>npm run lint</code>, <code>npm test</code> clean.</li></ol>\n<h2>Section 5 - test plan</h2>\n<ul><li>Unit: scripted replays (ignored-leak run, no-power-up run, clean run) produce the expected, evidence-cited tips; identical input gives identical output.</li></ul>\n<h2>Section 7 - dependencies and notes</h2>\n<p>Depends on FR-DD-AI-001 for the shared decision/seam types. This is the second concrete \"AI\" the player feels, and it is the bridge to FR-DD-EDU-001: in teams mode the same coach output becomes the after-action teaching summary.</p>";
