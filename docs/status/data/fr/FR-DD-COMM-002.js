window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-DD-COMM-002"]="<h2>Section 1 - behavior</h2>\n<p>A player can follow and unfollow another player from their public profile. A following feed shows recent public activity (new high scores, achievements, notable runs) from the people they follow, newest first. Profiles show follower and following counts. Private profiles never appear in a feed. Follow and unfollow are idempotent; self-follow and duplicate edges are rejected.</p>\n<h2>Section 4 - acceptance criteria</h2>\n<ol><li>Follow then unfollow returns to the start state; repeating either is a no-op (idempotent).</li><li>The feed shows only public activity from followed players, newest first.</li><li>A private profile contributes nothing to any feed.</li><li>Self-follow and duplicate-follow are rejected (unique edge).</li><li><code>npx tsc --noEmit</code>, <code>npm run lint</code>, <code>npm test</code> clean.</li></ol>\n<h2>Section 5 - test plan</h2>\n<ul><li>Unit: the follow edge enforces uniqueness and rejects self-follow.</li><li>Integration: A follows B, B's public run appears in A's feed; B goes private, it disappears.</li></ul>\n<h2>Section 7 - dependencies and notes</h2>\n<p>Depends on FR-DD-COMM-001. Keep the edge model simple (a <code>Follow</code> collection) so guilds (FR-DD-COMM-003) and referrals (FR-DD-SOC-004) can build on the same graph.</p>\n<h2>Section 8 - implementation status (2026-06-24, scaffolded)</h2>\n<p>Built and unit-tested (7 tests green):</p>\n<ul><li>lib/social/follow.ts: canFollow (rejects self-follow and empty ids), sortFeedNewestFirst (newest-first, bounded, ISO timestamps), dedupeIds. tests/follow.test.ts covers all three.</li></ul>\n<p>Server + UI (build-verified on the next-16 branch, not in the sandbox):</p>\n<ul><li>models/Follow.ts: follower -&gt; following edge with a unique compound index, so a duplicate follow is a no-op at the database level.</li><li>app/api/follow/route.ts: POST follow/unfollow (idempotent via upsert / deleteOne; self-follow rejected by canFollow) and GET ?username= returning follower/following counts, the viewer's follow state, isSelf, and canFollow.</li><li>app/api/feed/route.ts: GET returns recent runs from followed players, filtered to public profiles only (a private followed player contributes nothing), newest first via sortFeedNewestFirst.</li><li>app/feed/page.tsx: the following feed (sign-in gated).</li><li>components/FollowButton.tsx: counts + follow toggle, loaded client-side so the profile page stays a server component.</li><li>app/u/[username]/page.tsx: renders the FollowButton in the header.</li><li>components/Nav.tsx: Feed entry (signed-in only).</li><li>Discoverability win folded in: usernames on the leaderboard (app/leaderboard/page.tsx) and the tournament board (app/tournament/page.tsx) now link to /u/[username].</li></ul>\n<p>Acceptance mapping: idempotent follow/unfollow (upsert + unique index), feed shows only public followed activity newest-first (feed route + sortFeedNewestFirst), private profile excluded (profilePublic filter in the feed query), self-follow and duplicate rejected (canFollow + unique index). The follow/unfollow idempotence and the private-exclusion are integration behaviors; unit coverage is on the pure invariants. tsc/lint/build on the next-16 branch is the real gate for the model, the two routes, the pages, and the board edits.</p>\n<p>Follow-up: the feed is a flat \"recent runs\" list; richer activity types (achievements unlocked, new personal bests called out) can layer on later without changing the edge model.</p>";
