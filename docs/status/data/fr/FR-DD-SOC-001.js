window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-DD-SOC-001"]="<h2>Section 1 - behavior</h2>\n<p>From any finished run, the player can create a challenge link. The link encodes the run's seed and the player's score (the target) plus their username. When a friend opens it, they land on a challenge page that explains \"Beat &lt;name&gt;'s &lt;score&gt;\", then plays the identical seeded bug pattern. On finishing, the result screen compares their score to the target and offers a one-tap rematch or a re-challenge back.</p>\n<p>The embedded target is for display and motivation only. Any score that affects the real leaderboard still goes through <code>/api/scores</code> and is subject to NFR-DOM-001 server validation, so a forged token cannot inject a fake leaderboard entry.</p>\n<h2>Section 4 - acceptance criteria</h2>\n<ol><li>A challenge link round-trips: encode(seed, score, name) -&gt; decode returns the same values; tampered tokens fail to decode cleanly. Verified by <code>challenge.test.ts</code>.</li><li>Opening a challenge sets the exact seed; two players on the same link get the identical bug sequence (reuses the daily/seed determinism).</li><li>The result screen shows beat / missed against the target and offers rematch + re-challenge.</li><li>A forged target score never appears on the global leaderboard (it is display-only).</li><li><code>npx tsc --noEmit</code>, <code>npm run lint</code>, <code>npm test</code> clean.</li></ol>\n<h2>Section 5 - test plan</h2>\n<ul><li>Unit: token encode/decode round-trip and tamper rejection.</li><li>Integration: open a challenge URL, assert the seed and target render; finishing posts a normal score through the validated path, not the token.</li></ul>\n<h2>Section 7 - dependencies and notes</h2>\n<p>Depends on NFR-DOM-001 so the target a challenge brags about is a real score, not a fabricated one. Pairs with FR-DD-SOC-002 (per-run OG images) so the link unfurls with an image in chats and social posts - that is what makes it spread.</p>";
