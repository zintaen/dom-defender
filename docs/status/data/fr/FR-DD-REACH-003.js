window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-DD-REACH-003"]="<h2>Section 1 - behavior</h2>\n<p>Bring the game to a WCAG-aligned baseline (the measurable target is NFR-DOM-008). Full play is operable from the keyboard (mostly true today - close the gaps and add visible focus states). Honor <code>prefers-reduced-motion</code> with a calmer mode that removes flashing and softens the bug animations without changing difficulty. Fix color contrast on the UI and the bug palettes to AA. Add ARIA roles and labels to menus, the tool switcher, and dialogs so a screen reader can navigate the non-gameplay surfaces.</p>\n<p>Reduced motion changes presentation only; spawn rates and balance are untouched, so leaderboard fairness holds.</p>\n<h2>Section 4 - acceptance criteria</h2>\n<ol><li>A full run is completable using only the keyboard, with a visible focus indicator at all times.</li><li>With <code>prefers-reduced-motion: reduce</code>, the calmer mode is active: no flashing, gentler motion.</li><li>UI text and interactive controls meet AA contrast; bug colors are distinguishable on each skin.</li><li>axe reports no critical issues on the home, play, and leaderboard routes (NFR-DOM-008 gate).</li><li><code>npx tsc --noEmit</code>, <code>npm run lint</code>, <code>npm test</code> clean.</li></ol>\n<h2>Section 5 - test plan</h2>\n<ul><li>Automated: axe checks on key routes in CI (the NFR-DOM-008 gate).</li><li>Manual: keyboard-only run; reduced-motion run; a contrast check on each skin.</li></ul>\n<h2>Section 7 - dependencies and notes</h2>\n<p>Depends on NFR-DOM-008, which defines the measurable a11y gate this FR satisfies. Pairs with FR-DD-REACH-001: large, clearly-focused touch targets serve both touch and keyboard users.</p>";
